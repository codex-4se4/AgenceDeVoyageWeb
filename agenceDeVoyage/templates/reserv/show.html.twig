{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Contratfin</th>
                <td>{{ reservation.contratfin ? reservation.contratfin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Contratdebut</th>
                <td>{{ reservation.contratdebut ? reservation.contratdebut|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reserv_index') }}">back to list</a>

    <a href="{{ path('reserv_edit', {'id': reservation.id}) }}">edit</a>

    {{ include('reserv/_delete_form.html.twig') }}
{% endblock %}
